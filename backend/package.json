{
  "type": "module",
  "scripts": {
    "start": "npx prisma db push --accept-data-loss && node server.js",
    "dev": "concurrently \"npm run start:ml\" \"nodemon server.js\"",
    "start:ml": "conda run -n crowdscope uvicorn app:app --app-dir ../python --host 127.0.0.1 --port 8000"
  },
  "dependencies": {
    "@google/genai": "^0.8.0",
    "@prisma/client": "^5.22.0",
    "axios": "^1.7.9",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "p-limit": "^7.3.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "prisma": "^5.22.0"
  }
}
